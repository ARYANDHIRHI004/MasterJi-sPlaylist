import { create } from "zustand";

const useAlbums = create((set) => ({
    severalAlbums: null,
    gettingSeveralAlbums: false,

    getSeveralAlbums: async () => {
        try {
            1
        } catch (error) {
            
        }
    }
}))